services:
  app:
    build:
      context: ./app
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    env_file:
      - app/.env
    environment:
      - LOG_PATH=/app/logs/
    volumes:
      - ./app/logs:/app/logs
